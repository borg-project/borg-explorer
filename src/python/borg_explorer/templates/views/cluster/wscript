def build(context):
    context.install_files(
        "${PYTHONDIR}/borg_explorer/templates/views/cluster",
        [
            "selection.js",
            "cost_plot.js",
            "success_plot.js",
            ]
        )

